<p><%= subtitle(tour, date) %></p>

<h3>The Podium</h3>
<table class="table table-striped table-sm">
  <thead>
    <tr>
      <th/>
      <th class="text-left">Player</th>
      <th class="text-right">Points</th>
      <th class="text-right">Points Behind</th>
      <th class="text-right">Overall Rank</th>
      <th class="text-right">Previous Rank</th>
      <th class="text-right">Rank Change</th>
      <th class="text-right">Points Behind Overall</th>
    </tr>
  </thead>
  <tbody>
    <% leading_score = @podium.first.points %>
    <% podium.each_with_index do |result, index| %>
      <tr>
        <td class="text-right"><%= result.ordinal || ' ' %></td>
        <td class="text-left"><a href="/teams/<%= @year %>/<%= result.player.id %>"><%= result.player.name %></a></td>
        <td class="text-right"><%= result.points %></td>
        <td class="text-right"><%= leading_score - result.points %></td>
        <td class="text-right"><%= result.overall_rank %></td>
        <td class="text-right"><%= result.previous_rank %></td>
        <td class="text-right"><%= rank_change(result) %></td>
        <td class="text-right"><%= -1 * result.gap %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h3>The Wannabes</h3>

<% if wannabees.count > 0 %>
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th/>
        <th class="text-left">Player</th>
        <th class="text-right">Points</th>
        <th class="text-right">Points Behind</th>
        <th class="text-right">Overall Rank</th>
        <th class="text-right">Previous Rank</th>
        <th class="text-right">Rank Change</th>
        <th class="text-right">Points Behind Overall</th>
      </tr>
    </thead>
    <tbody>
      <% leading_score = podium.first.points %>
      <% wannabees.each_with_index do |result, index| %>
        <tr>
          <td class="text-right"><%= result.ordinal || ' ' %></td>
          <td class="text-left"><a href="/teams/<%= @year %>/<%= result.player.id %>"><%= result.player.name %></a></td>
          <td class="text-right"><%= result.points %></td>
          <td class="text-right"><%= leading_score - result.points %></td>
          <td class="text-right"><%= result.overall_rank %></td>
          <td class="text-right"><%= result.previous_rank %></td>
          <td class="text-right"><%= rank_change(result) %></td>
          <td class="text-right"><%= -1 * result.gap %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<% if peleton.count > 0 %>
  <h3>The Peloton</h3>

  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th/>
        <th class="text-left">Player</th>
        <th class="text-right">Points</th>
        <th class="text-right">Points Behind</th>
        <th class="text-right">Overall Rank</th>
        <th class="text-right">Previous Rank</th>
        <th class="text-right">Rank Change</th>
        <th class="text-right">Points Behind Overall</th>
      </tr>
    </thead>
    <tbody>
      <% leading_score = podium.first.points %>
      <% peleton.each_with_index do |result, index| %>
        <% if index == peleton.count - 1 %>
        <tr style="background-color: red;">
        <% else %>
        <tr>
        <% end %>
          <td class="text-right"><%= result.ordinal %></td>
          <td class="text-left"><a href="/teams/<%= @year %>/<%= result.player.id %>"><%= result.player.name %></a></td>
          <td class="text-right"><%= result.points %></td>
          <td class="text-right"><%= leading_score - result.points %></td>
          <td class="text-right"><%= result.overall_rank %></td>
          <td class="text-right"><%= result.previous_rank %></td>
          <td class="text-right"><%= rank_change(result) %></td>
          <td class="text-right"><%= -1 * result.gap %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
